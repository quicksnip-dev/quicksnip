export type ExtensionType = {
  icon: string;
  name: string;
  description: string;
  guide_url: string;
  source_url: string;
  downloads: number;
};

export type SnippetType = {
  title: string;
  languages: string[];
};
